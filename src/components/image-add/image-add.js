import React from "react";

const AddForm = () => {
    return (
        <form>
            <input type="text"></input>
            <button>Добавить</button>
        </form>
    );
};

export default AddForm;
